<?xml version="1.0" encoding="utf-8"?>
<com.atleta.customview.TabBar
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/tab_bar_height"
    android:background="@drawable/ripple_effect"
    android:gravity="center"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/image"
        app:tint="@drawable/nav_item_color_state"
        tools:src="@drawable/ic_skyhawk_grey"/>

    <com.atleta.customview.AtletaTextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/margin_default_quarter"
        android:textColor="@drawable/nav_item_color_state"
        android:textSize="10sp"
        app:appFont="Poppins-Regular"
        tools:text="@string/app_name"
        tools:ignore="SmallSp" />

</com.atleta.customview.TabBar>
